@model int?
@if (Model.HasValue && Model.Value > 0)
{
    <span class="badge badge-danger" id="notif-count">@Model.Value</span>
}
else
{
    <span class="badge badge-secondary" id="notif-count" style="display:none;">0</span>
}